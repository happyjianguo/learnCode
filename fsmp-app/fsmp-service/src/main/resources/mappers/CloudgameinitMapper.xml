<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.com.jansh.mapper.component.CloudgameinitMapper">

	<select id="select" resultType="CloudgameinitEntity">
		
		SELECT
			ID,INDEXNUM,CHANNEL,PLAYNAME,DEPLOYPATH,STATUS,CREATETIME,UPDATETIME,OPERATOR,RELESEACTIVITYURL
		FROM
			CLOUDGAMEINIT
		WHERE
			1=1
			<if test="id != null and id !=''">
				AND ID = #{id}
			</if>
			<if test="status != null and status !=''">
				AND STATUS = #{status}
			</if>
	</select>
	
	<select id="selectOne" resultType="CloudgameinitEntity">
		
		SELECT
			<!-- ID,INDEXNUM,CHANNEL,PLAYNAME,DEPLOYPATH,STATUS,CREATETIME,UPDATETIME,OPERATOR -->
			ID,INDEXNUM,CHANNEL,PLAYNAME,DEPLOYPATH,STATUS,CREATETIME,UPDATETIME,OPERATOR,RELESEACTIVITYURL
		FROM
			CLOUDGAMEINIT
		WHERE
			1=1
			<if test="id != null and id !=''">
				AND ID = #{id}
			</if>
	</select>

</mapper>