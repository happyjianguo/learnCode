<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.com.jansh.mapper.wsfdn.CfPhoneNoInfoMapper">
	<select id="queryByPhone" parameterType="String" resultType="CfPhoneNoInfoEntity">
		SELECT 
			PHONE,
			SUPPLIER,
			PROVINCE,
			CITY
		FROM
			CF_PHONENOINFO
		WHERE
			PHONE = #{phone}
	</select>
	<insert id="save" parameterType="CfPhoneNoInfoEntity">
		INSERT INTO CF_PHONENOINFO
			(
			PHONE,
			SUPPLIER,
			PROVINCE,
			CITY
			)
			VALUES
			(
			#{phone},
			#{supplier},
			#{province},
			#{city}
			)

	</insert>

</mapper>