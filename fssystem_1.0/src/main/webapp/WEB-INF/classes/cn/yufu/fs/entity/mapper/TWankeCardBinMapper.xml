<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.yufu.fs.dao.TWankeCardBinMapper" >
  
  <resultMap id="BaseResultMap" type="cn.yufu.fs.entity.TWankeCardBin" >
    <id column="CARD_ID" property="cardId" jdbcType="VARCHAR" />
    <result column="CARD_BIN" property="cardBin" jdbcType="VARCHAR" />
  </resultMap>
  
  <sql id="Example_Where_Clause" >
    <where >
      <foreach collection="example.oredCriteria" item="criteria" separator="or" >
        <if test="criteria.valid" >
          <trim prefix="(" suffix=")" prefixOverrides="and" >
            <foreach collection="criteria.criteria" item="criterion" >
              <choose >
                <when test="criterion.noValue" >
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue" >
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue" >
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue" >
                  and ${criterion.condition}
                  <foreach collection="criterion.value" item="listItem" open="(" close=")" separator="," >
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  
  <sql id="Base_Column_List" >
    CARD_ID, CARD_BIN
  </sql>
  
  <select id="getCardId" resultType="java.lang.String">
    SELECT MAX(CARD_ID) + 1 AS CARD_ID FROM T_WANKE_CARDBIN
  </select>
  
  <select id="selectByPrimaryKey" resultMap="BaseResultMap">
    SELECT 
    	<include refid="Base_Column_List" />
    FROM T_WANKE_CARDBIN WHERE CARD_ID = #{cardId,jdbcType=VARCHAR}
  </select>
  
  <select id="countByExample" parameterType="cn.yufu.fs.entity.TWankeCardBinExample" resultType="java.lang.Integer">
    SELECT COUNT(*) FROM T_WANKE_CARDBIN
	<if test="_parameter != null" >
      <include refid="Example_Where_Clause" />
    </if>
  </select>
  
  <select id="selectByExample" resultMap="BaseResultMap" parameterType="cn.yufu.fs.entity.TWankeCardBinExample" >
    SELECT
    	<include refid="Base_Column_List" />
    FROM T_WANKE_CARDBIN
    <if test="_parameter != null" >
      <include refid="Example_Where_Clause" />
    </if>
    <if test="example.orderByClause != null" >
      ORDER BY ${example.orderByClause}
    </if>
  </select>
  
  <select id="selectPageList" parameterType="map" resultMap="BaseResultMap">
	SELECT * FROM (
	SELECT a.*, ROWNUM rn FROM(
	SELECT 
		<include refid="Base_Column_List" />
	FROM T_WANKE_CARDBIN
		<if test="_parameter != null" >
	      <include refid="Example_Where_Clause" />
	    </if> 
	    <if test="example.orderByClause != null" >
	      ORDER BY ${example.orderByClause}
	    </if> ) a
		WHERE <![CDATA[ ROWNUM <= ${endResult} ]]>)
		WHERE rn &gt; ${startResult}
  </select>
  
  <insert id="insertCardBin" parameterType="cn.yufu.fs.entity.TWankeCardBin" >
    INSERT INTO T_WANKE_CARDBIN (CARD_ID, CARD_BIN)
    VALUES (#{cardId,jdbcType=VARCHAR}, #{cardBin,jdbcType=VARCHAR})
  </insert>
  
  <update id="updateCardBin" parameterType="cn.yufu.fs.entity.TWankeCardBin" >
    UPDATE T_WANKE_CARDBIN
    <set >
      <if test="cardBin != null" >
        CARD_BIN = #{cardBin,jdbcType=VARCHAR},
      </if>
    </set>
    WHERE CARD_ID = #{cardId,jdbcType=VARCHAR}
  </update>

  <delete id="deleteByPrimaryKey" parameterType="java.lang.String" >
    DELETE FROM T_WANKE_CARDBIN  WHERE CARD_ID = #{cardId,jdbcType=VARCHAR}
  </delete>
  
</mapper>