<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.yufu.bak.dao.TransactionrecordsDailyMapper" >
  
  <resultMap id="BaseResultMap" type="cn.yufu.bak.entity.TransactionrecordsDaily" >
    <result column="MERCHANTNUMBER" property="merchantnumber" jdbcType="VARCHAR" />
    <result column="TRANSACTIONDATE" property="transactiondate" jdbcType="VARCHAR" />
    <result column="TRANSACTIONMONEY_SUM" property="transactionmoneySum" jdbcType="DECIMAL" />
    <result column="MOMMENTS1" property="momments1" jdbcType="VARCHAR" />
    <result column="MOMMENTS2" property="momments2" jdbcType="VARCHAR" />
    <result column="MOMMENTS3" property="momments3" jdbcType="VARCHAR" />
    <result column="TRAN_NUM" property="tranNum" jdbcType="DECIMAL" />
  </resultMap>
  
  <sql id="Example_Where_Clause" >
    <where >
      <foreach collection="example.oredCriteria" item="criteria" separator="or" >
        <if test="criteria.valid" >
          <trim prefix="(" suffix=")" prefixOverrides="and" >
            <foreach collection="criteria.criteria" item="criterion" >
              <choose >
                <when test="criterion.noValue" >
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue" >
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue" >
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue" >
                  and ${criterion.condition}
                  <foreach collection="criterion.value" item="listItem" open="(" close=")" separator="," >
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Update_By_Example_Where_Clause" >
    <where >
      <foreach collection="example.oredCriteria" item="criteria" separator="or" >
        <if test="criteria.valid" >
          <trim prefix="(" suffix=")" prefixOverrides="and" >
            <foreach collection="criteria.criteria" item="criterion" >
              <choose >
                <when test="criterion.noValue" >
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue" >
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue" >
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue" >
                  and ${criterion.condition}
                  <foreach collection="criterion.value" item="listItem" open="(" close=")" separator="," >
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  
  <sql id="Base_Column_List" >
    MERCHANTNUMBER, SUM(TRANSACTIONMONEY_SUM) AS TRANSACTIONMONEY_SUM, 
    SUM(TRAN_NUM) AS TRAN_NUM
  </sql>
  
  <select id="selectByExample" resultMap="BaseResultMap">
    select
    <if test="example.distinct" >
      example.distinct
    </if>
    <include refid="Base_Column_List" />
    from TRANSACTIONRECORDS_DAILY
    <if test="_parameter != null" >
      <include refid="Example_Where_Clause" />
    </if>
    GROUP BY MERCHANTNUMBER 
    <if test="example.orderByClause != null" >
      order by ${example.orderByClause}
    </if>
  </select>
  
  <select id="selectPageList" parameterType="map" resultMap="BaseResultMap">
	SELECT * FROM (
	SELECT a.*, ROWNUM rn FROM(
	SELECT 
		<include refid="Base_Column_List" />
    FROM TRANSACTIONRECORDS_DAILY 
	<if test="_parameter != null">
      	<include refid="Example_Where_Clause" />
    </if>
    GROUP BY MERCHANTNUMBER 
    <if test="example.orderByClause != null">
      	order by ${example.orderByClause}
    </if> ) a
		WHERE <![CDATA[ ROWNUM <= ${endResult} ]]>)
		WHERE rn &gt; ${startResult}
  </select>  
 
  <select id="countByExample" resultType="java.lang.Integer" >
    select count(*) FROM (
    SELECT     
    <include refid="Base_Column_List" />
    from TRANSACTIONRECORDS_DAILY
    <if test="_parameter != null" >
      <include refid="Example_Where_Clause" />
    </if>
    GROUP BY MERCHANTNUMBER )
  </select>
  
  <select id="getNumAndAmt" resultMap="BaseResultMap">
    select SUM(TRANSACTIONMONEY_SUM) AS TRANSACTIONMONEY_SUM, 
    SUM(TRAN_NUM) AS TRAN_NUM 
    from TRANSACTIONRECORDS_DAILY
    <if test="_parameter != null" >
      <include refid="Example_Where_Clause" />
    </if>
  </select>
 
</mapper>