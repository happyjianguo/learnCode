<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.yufu.fs.dao.TCardTypeConversionMapper" >

  <resultMap id="BaseResultMap" type="cn.yufu.fs.entity.TCardTypeConversion" >
    <result column="CARDTYPE" property="cardtype" jdbcType="DECIMAL" />
    <result column="CARDTYPENAME" property="cardtypename" jdbcType="CHAR" />
    <result column="CARDTYPE_ID" property="cardtypeId" jdbcType="DECIMAL" />
    <result column="CARDTYPE_ID_NAME" property="cardtypeIdName" jdbcType="CHAR" />
  </resultMap>
  
  <sql id="Example_Where_Clause" >
    <where >
      <foreach collection="example.oredCriteria" item="criteria" separator="or" >
        <if test="criteria.valid" >
          <trim prefix="(" suffix=")" prefixOverrides="and" >
            <foreach collection="criteria.criteria" item="criterion" >
              <choose >
                <when test="criterion.noValue" >
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue" >
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue" >
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue" >
                  and ${criterion.condition}
                  <foreach collection="criterion.value" item="listItem" open="(" close=")" separator="," >
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  
  <sql id="Base_Column_List" >
    CARDTYPE, CARDTYPENAME, CARDTYPE_ID, CARDTYPE_ID_NAME
  </sql>
  
  <select id="get" resultMap="BaseResultMap">
    SELECT 
    	<include refid="Base_Column_List" />
    FROM T_CARDTYPE_CONVERSION WHERE CARDTYPE = #{cardtype,jdbcType=DECIMAL}
  </select>
  
  <select id="countByExample" parameterType="cn.yufu.fs.entity.TCardTypeConversionExample" resultType="java.lang.Integer">
    SELECT COUNT(*) FROM T_CARDTYPE_CONVERSION
	<if test="_parameter != null" >
      <include refid="Example_Where_Clause" />
    </if>
  </select>
  
  <select id="selectByExample" resultMap="BaseResultMap" parameterType="cn.yufu.fs.entity.TCardTypeConversionExample" >
    SELECT
    	<include refid="Base_Column_List" />
    FROM T_CARDTYPE_CONVERSION
	    <if test="_parameter != null" >
	      <include refid="Example_Where_Clause" />
	    </if>
	    <if test="example.orderByClause != null" >
	      ORDER BY ${example.orderByClause}
	    </if>
  </select>
  
  <select id="selectPageList" parameterType="map" resultMap="BaseResultMap">
	SELECT * FROM (
	SELECT a.*, ROWNUM rn FROM(
	SELECT 
		<include refid="Base_Column_List" />
	FROM T_CARDTYPE_CONVERSION
		<if test="_parameter != null" >
	      <include refid="Example_Where_Clause" />
	    </if> 
	    <if test="example.orderByClause != null" >
	      ORDER BY ${example.orderByClause}
	    </if> ) a
		WHERE <![CDATA[ ROWNUM <= ${endResult} ]]>)
		WHERE rn &gt; ${startResult}
  </select>
  
  <insert id="insertCardType" parameterType="cn.yufu.fs.entity.TCardTypeConversion" >
    insert into T_CARDTYPE_CONVERSION (CARDTYPE, CARDTYPENAME, CARDTYPE_ID, 
      CARDTYPE_ID_NAME)
    values (#{cardtype,jdbcType=DECIMAL}, #{cardtypename,jdbcType=CHAR}, 
    		#{cardtypeId,jdbcType=DECIMAL}, #{cardtypeIdName,jdbcType=CHAR})
  </insert>
  
  <update id="updateCardType" parameterType="cn.yufu.fs.entity.TCardTypeConversion" >
    UPDATE T_CARDTYPE_CONVERSION 
    SET CARDTYPE = #{cardtype,jdbcType=DECIMAL}, 
    	CARDTYPENAME = #{cardtypename,jdbcType=CHAR}, 
    	CARDTYPE_ID = #{cardtypeId,jdbcType=DECIMAL}, 
      	CARDTYPE_ID_NAME = #{cardtypeIdName,jdbcType=CHAR}
    WHERE CARDTYPE = #{cardtype,jdbcType=DECIMAL}  	
  </update>
  
  <delete id="deleteByPrimaryKey">
    DELETE FROM T_CARDTYPE_CONVERSION WHERE CARDTYPE = #{cardtype,jdbcType=DECIMAL}
  </delete>
</mapper>